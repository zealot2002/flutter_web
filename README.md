# Flutter Unit Web 版本

Flutter Unit 是一个全面的 Flutter Widget 演示应用，包含 100+ 个常用组件的交互式示例和详细文档。

本仓库为 Flutter Unit 的 Web 版本构建，可以直接在浏览器中查看所有 Flutter Widget 的演示效果。

## ✨ 功能特点

- 📱 **100+ Flutter Widget 演示** - 涵盖常用的 Flutter 组件
- 🎯 **分类清晰** - 按无态、有态、单道、多道等分类
- 📖 **详细文档** - 每个组件都有详细说明和使用示例
- 🎨 **实时预览** - 直接在浏览器中查看组件渲染效果
- 🔗 **关联推荐** - 提供相关组件的快速跳转
- ⭐ **评分系统** - 组件重要性星级评分

## 🚀 快速开始

### 方法一：使用 Python（推荐）

确保你的系统已安装 Python 3，然后在项目目录中运行：

```bash
# 进入项目目录
cd flutter_demos

# 启动 HTTP 服务器
python3 -m http.server 8000
```

然后在浏览器中访问：**http://localhost:8000**

### 方法二：使用 Node.js

如果你安装了 Node.js，可以使用 `http-server`：

```bash
# 安装 http-server（首次使用需要安装）
npm install -g http-server

# 启动服务器
http-server -p 8000
```

然后在浏览器中访问：**http://localhost:8000**

### 方法三：使用 PHP

如果你的系统安装了 PHP：

```bash
php -S localhost:8000
```

然后在浏览器中访问：**http://localhost:8000**

## 📁 项目结构

```
flutter_demos/
├── index.html              # 主页面入口
├── main.dart.js           # Flutter 编译后的 JavaScript 代码
├── flutter.js             # Flutter Web 引擎
├── flutter_bootstrap.js   # 启动引导脚本
├── manifest.json          # Web 应用清单
├── assets/                # 资源文件
│   ├── assets/           # 图片、字体等资源
│   ├── fonts/            # 字体文件
│   └── shaders/          # 着色器文件
├── canvaskit/            # CanvasKit 渲染引擎
└── icons/                # 应用图标
```

## 🎯 主要功能模块

### 1. 组件集录
- 展示所有 Flutter Widget 的卡片式目录
- 支持按分类筛选（无态、有态、单道、多道、滑片、代理、其他）
- 支持搜索功能

### 2. 组件详情
- 详细的组件说明
- 实时交互演示
- 相关组件推荐
- 代码示例展示

### 3. 其他功能
- 留言板
- 知识集锦
- 绘制集录
- 工具宝箱
- 应用信息

## 🌐 技术栈

- **Flutter** - 使用 Flutter 3.35.4 构建
- **CanvasKit** - 高性能 Web 渲染引擎
- **Dart** - 编译为 JavaScript

## 📝 注意事项

1. **端口占用**：如果 8000 端口被占用，可以更换其他端口：
   ```bash
   python3 -m http.server 9000
   ```

2. **浏览器兼容性**：建议使用现代浏览器（Chrome、Firefox、Safari、Edge）

3. **本地开发**：由于浏览器安全限制，必须通过 HTTP 服务器访问，直接双击打开 HTML 文件可能无法正常工作

4. **移动端访问**：可以通过局域网 IP 地址在移动设备上访问：
   ```bash
   # 查看本机 IP 地址
   ifconfig | grep "inet "
   
   # 然后在移动设备浏览器中访问
   # http://你的IP地址:8000
   ```

## 📦 构建说明

本项目是从 Flutter Unit 源码构建而来：

```bash
# 克隆 Flutter Unit 源码
git clone https://github.com/toly1994328/FlutterUnit.git

# 进入项目目录
cd FlutterUnit

# 构建 Web 版本
flutter build web --release

# 构建产物位于 build/web/ 目录
```

## 🔗 相关链接

- **Flutter Unit 原项目**: [https://github.com/toly1994328/FlutterUnit](https://github.com/toly1994328/FlutterUnit)
- **Flutter 官方文档**: [https://flutter.dev](https://flutter.dev)
- **Flutter Widget 目录**: [https://flutter.dev/docs/development/ui/widgets](https://flutter.dev/docs/development/ui/widgets)

## 📄 许可证

本项目基于 Flutter Unit 构建，遵循原项目的开源协议。

## 🙏 致谢

感谢 [张风捷特烈](https://github.com/toly1994328) 开发的 Flutter Unit 项目！

---

**享受 Flutter 学习之旅！** 🚀✨

